type: string
enum:
  - INCOMING_PAYMENT
  - OUTGOING_PAYMENT
  - TEST
  - BULK_UPLOAD
  - INVITATION_CLAIMED
  - KYC_STATUS
  - ACCOUNT_STATUS
description: >-
  Type of webhook event, used by the receiver to identify which webhook is being
  received
