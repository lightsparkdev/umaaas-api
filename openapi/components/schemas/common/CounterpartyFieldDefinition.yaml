type: object
properties:
  name:
    $ref: ../users/UserInfoFieldName.yaml
  mandatory:
    type: boolean
    description: Whether the field is mandatory
    example: true
required:
  - name
  - mandatory
