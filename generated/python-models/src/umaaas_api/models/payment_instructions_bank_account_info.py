# coding: utf-8

"""
    UMA as a Service (UMAaaS) API

    API for managing global payments to and from UMA addresses.  This service facilitates cross-currency financial transactions using simple human-readable UMA addresses. 

    The version of the OpenAPI document: 1.0.0
    Contact: support@lightspark.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
from inspect import getfullargspec
import json
import pprint
import re  # noqa: F401



from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from umaaas_api.models.clabe_account_info import ClabeAccountInfo
from umaaas_api.models.iban_account_info import IbanAccountInfo
from umaaas_api.models.pix_account_info import PixAccountInfo
from umaaas_api.models.us_account_info import UsAccountInfo
from typing import Union, Any, List, TYPE_CHECKING, Optional, Dict
from typing_extensions import Literal
from pydantic import StrictStr, Field
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

PAYMENTINSTRUCTIONSBANKACCOUNTINFO_ONE_OF_SCHEMAS = ["ClabeAccountInfo", "IbanAccountInfo", "PixAccountInfo", "UsAccountInfo"]

class PaymentInstructionsBankAccountInfo(BaseModel):
    """
    PaymentInstructionsBankAccountInfo
    """
    # data type: ClabeAccountInfo
    oneof_schema_1_validator: Optional[ClabeAccountInfo] = None
    # data type: UsAccountInfo
    oneof_schema_2_validator: Optional[UsAccountInfo] = None
    # data type: PixAccountInfo
    oneof_schema_3_validator: Optional[PixAccountInfo] = None
    # data type: IbanAccountInfo
    oneof_schema_4_validator: Optional[IbanAccountInfo] = None
    actual_instance: Optional[Union[ClabeAccountInfo, IbanAccountInfo, PixAccountInfo, UsAccountInfo]] = None
    one_of_schemas: List[str] = Literal["ClabeAccountInfo", "IbanAccountInfo", "PixAccountInfo", "UsAccountInfo"]

    model_config = {
        "validate_assignment": True,
        "protected_namespaces": (),
    }


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = PaymentInstructionsBankAccountInfo.model_construct()
        error_messages = []
        match = 0
        # validate data type: ClabeAccountInfo
        if not isinstance(v, ClabeAccountInfo):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ClabeAccountInfo`")
        else:
            match += 1
        # validate data type: UsAccountInfo
        if not isinstance(v, UsAccountInfo):
            error_messages.append(f"Error! Input type `{type(v)}` is not `UsAccountInfo`")
        else:
            match += 1
        # validate data type: PixAccountInfo
        if not isinstance(v, PixAccountInfo):
            error_messages.append(f"Error! Input type `{type(v)}` is not `PixAccountInfo`")
        else:
            match += 1
        # validate data type: IbanAccountInfo
        if not isinstance(v, IbanAccountInfo):
            error_messages.append(f"Error! Input type `{type(v)}` is not `IbanAccountInfo`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in PaymentInstructionsBankAccountInfo with oneOf schemas: ClabeAccountInfo, IbanAccountInfo, PixAccountInfo, UsAccountInfo. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in PaymentInstructionsBankAccountInfo with oneOf schemas: ClabeAccountInfo, IbanAccountInfo, PixAccountInfo, UsAccountInfo. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: dict) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into ClabeAccountInfo
        try:
            instance.actual_instance = ClabeAccountInfo.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into UsAccountInfo
        try:
            instance.actual_instance = UsAccountInfo.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into PixAccountInfo
        try:
            instance.actual_instance = PixAccountInfo.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into IbanAccountInfo
        try:
            instance.actual_instance = IbanAccountInfo.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into PaymentInstructionsBankAccountInfo with oneOf schemas: ClabeAccountInfo, IbanAccountInfo, PixAccountInfo, UsAccountInfo. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into PaymentInstructionsBankAccountInfo with oneOf schemas: ClabeAccountInfo, IbanAccountInfo, PixAccountInfo, UsAccountInfo. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        to_json = getattr(self.actual_instance, "to_json", None)
        if callable(to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Dict:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        to_dict = getattr(self.actual_instance, "to_dict", None)
        if callable(to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


